disallowed-methods = [
    { path = "std::result::Result::expect", reason = "use proper error handling (see ADR-001)" },
    { path = "std::result::Result::unwrap", reason = "use proper error handling (see ADR-001)" },
    { path = "std::option::Option::expect", reason = "use proper error handling (see ADR-001)" },
    { path = "std::option::Option::unwrap", reason = "use proper error handling (see ADR-001)" },
]
