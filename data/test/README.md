# Test Data

`data/test/` is for small, deterministic fixtures that should live in git and be safe to use in local development and automated tests.

Use this directory for source inputs that help exercise ingest, retrieval, chat, or other MVP paths without depending on local-only data.

Do not put runtime state, generated indexes, large corpora, or private documents here.
